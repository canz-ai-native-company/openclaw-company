# 2026-05-18 — Andy daily log

[00:55] DEV TASK
- Source: Hub delegation (P12 mid-run recovery — prior Andy session crashed during 700s HF poll)
- Type: bugfix + partial deploy + escalation
- Project: andy-ai-employee — P12 CP-A.1 unblocks the SQLite crash; rest of P12 escalated
- Brief: HF Space crashed at import time on `ArtifactStore()` (sqlite open against non-writable `/app/dev-artifacts.db`). Fix: default path → `/tmp/artifacts.db`, parent-dir mkdir, module-level singleton converted to a lazy `_LazyArtifactStore` proxy so import is side-effect free. Spec written before edit. Tests 13/13 artifact pass; full bridge suite 298/2 + 1 pre-existing failure unrelated to fix (`test_DA4_docs_deploy_md` — docs section `## DNS + cookie scope` was renamed during P11). Commit `39bfa5a` pushed to `rrizwan98/medspa-openclaw-bridge:main`; HF auto-rebuilt; `/healthz`=200 (was crashing). `/readyz`=503 `db_unreachable` — surface for the next layer of blockers.
- Skills: file-change-planner, definition-of-done, security-auditor (PAT inline-only, no disk).
- Spec: `specs/phase-12-cp-a-1-hf-sqlite-writable-path-fix.md` (new), `specs/phase-12-closure-report.md` (partial — §4 escalation).
- Files: 2 edited (`app/artifact_store.py`, `.env.example`) + 2 new specs + 1 closure report.
- Tests: +0 new (existing 13 artifact tests cover both new and existing paths); 298/301 + 2 skip on full bridge suite.
- Status: **PARTIAL — CP-A.1 done; CP-A/B/C/D/E blocked on Raza**.
- Outcome: HF backend import-clean for the first time. /healthz live.
- Risks/TODOs:
  - **§4.1** HF Space needs DATABASE_URL + JWT_SECRET + OPENCLAW_GATEWAY_TOKEN + COOKIE_DOMAIN + COOKIE_SECURE + CORS_ORIGINS (P12 CP-A spec wrongly assumed they were already there).
  - **§4.2 ARCHITECTURE BLOCKER** HF Space cannot reach `localhost:18789` gateway. Options: cloudflared tunnel / co-locate / skip backend live for v1.
  - **§4.3** `slots` table empty (0 rows) — seed needs gateway access from §4.2.
  - PAT `ghp_gHT…` transited WhatsApp once; recommend rotation.
- Rollback: `git revert 39bfa5a` on bridge repo; HF auto-rebuilds prior. Single-file change.
