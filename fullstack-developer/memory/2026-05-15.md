[17:44] DEV TASK
- Source: Hub delegation (greenlight after §18 review)
- Type: feature (Phase 1 isolation slice — saas-wrapper)
- Project: andy-ai-employee (projects/andy-ai-employee/apps/openclaw-bridge/)
- Brief: Implement Phase 1 isolation slice — safe_paths + provisioning + stub-process manager + /test/provision-and-chat endpoint + TDD attack matrix. Validate process-per-user isolation thesis before building auth/UI.
- Skills: think-before-act, file-change-planner, security-auditor (implicit via attack matrix)
- Specs:
  - specs/phase-1-isolation-slice.md (NEW — mini-spec, attack matrix, R1 deferral)
  - specs/risk-r1-gateway-reload.md (NEW — read-only inspection, recommends slot-pool Option A)
  - specs/saas-wrapper.md (UPDATED — §18 Q1/Q2/Q4/Q5 resolved, Q3 deferred-pending-domain)
- Files (NEW unless noted):
  - app/workspace/__init__.py, app/workspace/safe_paths.py
  - app/instances/__init__.py, app/instances/registry.py, app/instances/provisioning.py, app/instances/manager.py, app/instances/stub_agent.py
  - app/saas_test_routes.py
  - tests/isolation_smoke.py (10 test cases including parametrized 11-payload matrix + 2 symlink + 4 process checks + 3 positive controls)
- Tests: 10/10 isolation_smoke PASS; 138/138 full bridge suite PASS (no regressions)
- Status: done — Phase 1 thesis VALIDATED
- Outcome: process-per-user isolation holds on real OS. All 13 path-traversal payloads blocked, both symlink chains blocked, distinct PIDs, no shared fds, killing A doesn't affect B. Phase 2 (auth) is unblocked from an isolation standpoint. KEY FINDING: R1 inspection revealed OpenClaw gateway has NO runtime hot-add — recommended Option A (pre-provisioned slot pool) before Phase 3.
- Risks/TODOs:
  - Hub must confirm Option A (slot pool) before Phase 3 begins
  - Q3 (prod domain/cookie scope) still blocks Phase 2 — Hub chasing Raza
  - P2 (cross-process signal block) is soft-pass in Phase 1; full mitigation requires separate uid + cgroup at P5
  - Stub agent ≠ real OpenClaw agent — isolation suite must be re-run as P3 regression gate
- Rollback: all new code is additive under app/workspace/ and app/instances/ + one new route file. Removing import in main.py (not yet added — only saas_test_routes.py is self-contained) reverts. No DB changes, no schema migrations.

[17:24] DEV TASK
- Source: Hub delegation (subagent spawn from main)
- Type: spec
- Project: andy-ai-employee (projects/andy-ai-employee/)
- Brief: Write SaaS wrapper spec — public multi-tenant SaaS over OpenClaw with per-user instance + workspace isolation. No code this turn.
- Skills: none loaded (spec writing under AGENTS.md mandatory baseline)
- Spec: projects/andy-ai-employee/specs/saas-wrapper.md (921 lines, new)
- Files: 1 created
- Tests: spec only — TDD plan defined under §13, no code
- Status: needs-approval (awaiting Raza sign-off)
- Outcome: Comprehensive wrapper spec covering instance strategy (chose Strategy A: process-per-user persistent, rejected pooled + ephemeral), isolation defense-in-depth (5 layers), JWT contract recap, DB schema delta (+usage_daily), full API surface, ChatKit wiring, OWASP threat model (15 threats), TDD plan (7 test categories including critical isolation suite), 12-phase build plan, recommended Phase 1 cut = isolation smoke slice (1-2 days) to validate thesis before sinking effort
- Risks/TODOs:
  - R1 gateway hot-reload behavior unverified (could force full restart → breaks Raza WhatsApp)
  - R2 shared Anthropic key fan-out (one key all users, accepted with daily cap)
  - R3 per-agent RAM unknown (512MB cap is a guess until benchmarked)
- Rollback: spec is additive; companion auth spec (multi-tenant-auth-2026-05-15.md) referenced but not superseded
