[20:08] DEV TASK
- Source: Hub delegation (Raza, via WhatsApp)
- Type: feature (specs-driven, Phases 0→3 in one session)
- Project: andy-ai-employee (apps/openclaw-bridge)
- Brief: Realtime progress via Gateway WS — bridge subscribes to chat events, translates tool_use → ChatKit WorkflowItem tasks, behind OPENCLAW_REALTIME_PROGRESS flag.
- Skills: think-before-act, file-change-planner, security-auditor (env+token), definition-of-done, env-secrets-manager, git-workflow
- Specs:
  • Created: ~/.openclaw/agents/fullstack-developer/workspace/projects/andy-ai-employee/specs/realtime-progress-via-gateway-ws.md
  • Amended: projects/andy-ai-employee/specs/realtime-progress-events.md §18
  • Phase 1 evidence: specs/m1-t13-ws-tap-evidence/ (ws_tap.py + FINDINGS.md)
  • Phase 3 evidence: specs/phase3-e2e-evidence/ (sse-stream.txt + 6 screenshots + REPORT.md)
- Files (new):
  • apps/openclaw-bridge/app/transcript_to_tasks.py
  • apps/openclaw-bridge/app/gateway_ws_client.py
  • apps/openclaw-bridge/app/realtime_progress_feed.py
  • apps/openclaw-bridge/tests/test_transcript_to_tasks.py (28 tests)
  • apps/openclaw-bridge/tests/test_gateway_ws_client.py (13 tests)
  • apps/openclaw-bridge/tests/test_realtime_progress_feed.py (9 tests)
- Files (edited):
  • apps/openclaw-bridge/app/openclaw_chatkit_server.py (respond() + watch_loop wiring, flag-gated)
  • apps/openclaw-bridge/tests/test_progress_workflow.py (+2 new tests)
  • apps/openclaw-bridge/.env.example (+OPENCLAW_REALTIME_PROGRESS=off)
- Tests: 130 passing (78 prior + 52 new). All M1 hard gates green (T-1…T-16).
- Status: done
- Outcome: WS-based realtime progress feed live behind feature flag (default off). Wire-level invariants proven via curl SSE. Phase-1 tap proved sanitizer-preservation of tool_use blocks via source review (chat-display-projection-BSsHGnx6.js:25-102).
- Risks/TODOs:
  • Live tool-task visual proof not captured (curl turn returned "(no response)" from OPENCLAW_MODEL=openclaw; tool-task render proven only by fake-gateway integration tests). User can flip flag to ws and visually verify when convenient.
  • `websockets` is a transitive dep via uvicorn[standard]; if uvicorn changes extras, add explicit dep to pyproject.toml.
  • Bridge file isn't git-tracked at HEAD; recommend committing andy-ai-employee project before any future risky change for cleaner rollback.
- Rollback:
  • Operational: OPENCLAW_REALTIME_PROGRESS=off + restart bridge → byte-identical (T-7 protected).
  • Code: manual revert of 8 hunks in openclaw_chatkit_server.py + rm of 3 new app files + 3 new test files.


# 2026-05-13 — Daily Log

[13:25] DEV TASK
- Source: Hub delegation (inter-session) — REVISED M1 follow-up (pivot away from full-screen modal)
- Type: feature (dedicated /chatkit route) + bugfix (Change 2 timestamp re-render regression)
- Project: andy-ai-employee
- Brief: Raza rejected the stretched modal ("yia is tarans say acha nh lag rha h"). Pivot: dedicated `/chatkit` page route (`apps/web/app/chatkit/page.tsx`) instead of modal. All "Talk to Andy" / "Hire Andy" CTAs rewired to `<Link href="/chatkit">` (Nav desktop+mobile, Hero, ChatLauncher). ChatLauncher reduced from drawer-modal-with-own-ChatKitMount to a thin floating Link; auto-hides on `/chatkit` and `/build/...`. The `andy:open-chat` event channel removed entirely.
  Change 2 (workflow widget below prose): previous fix (yield workflow ADDED before assistant ADDED) wasn't enough — ChatKit React sorts by `created_at` on re-render. New fix: defer assistant `created_at` and `AssistantMessageItem` construction INTO the try block AFTER `tracker.add_task("Thinking")` opens the workflow, so workflow.created_at < assistant.created_at. T-3 strengthened with timestamp invariant.
- Skills: think-before-act, file-change-planner
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (§17 added; 2 decision-log entries: pivot from modal, timestamp ordering)
- Files: `apps/web/app/chatkit/page.tsx` (new), `apps/web/components/ChatLauncher.tsx` (rewrite as Link, drawer removed), `apps/web/components/Nav.tsx` (2 buttons → Link), `apps/web/components/Hero.tsx` (button → Link), `apps/openclaw-bridge/app/openclaw_chatkit_server.py` (defer created_at + empty_item into try block), `apps/openclaw-bridge/tests/test_progress_workflow.py` (T-3 timestamp invariant), `apps/web/scripts/chatkit-route-check.mjs` (new visual smoke).
- Tests: 78/78 bridge tests passing incl. strengthened T-3 (now asserts both receive-order AND timestamp-order). Live curl: workflow=13:16:42.925242 < assistant=13:16:42.938545 (13 ms gap). Playwright at 375/768/1280 against /chatkit: workflow widget renders ABOVE actual prose ("Hi Raza — kya kaam hai?").
- Status: done
- Outcome: Both UX issues fully resolved. Full-page chat surface, no modal hacks. Workflow widget stays above prose even after re-render. Zero @openai/chatkit npm deps.
- Risks/TODOs: ChatDemo inline component still exists on landing page — unchanged (separate live-demo, not a CTA). Landing page still mounts ONE ChatKit instance (the demo); /chatkit route mounts its own. Acceptable.
- Rollback: `git restore` 5 touched files. ACTIVITY_EVENTS=off remains the safety net for the bridge timestamp change.

[13:10] DEV TASK
- Source: Hub delegation (inter-session) — M1 follow-up (2 UX fixes)
- Type: bugfix (Change 2 server-side ordering regression) + feature (Change 1 full-screen modal)
- Project: andy-ai-employee
- Brief: Change 2 — workflow widget was rendering BELOW assistant prose. Root cause: `respond()` yielded AssistantMessageItem ADDED before WorkflowItem ADDED (ChatKit renders in receive order). Fix: open workflow first via eager `tracker.add_task` BEFORE the empty assistant ADDED yield; defer assistant ADDED into try block; except path adds it just-in-time if exception fires early. T-3 strengthened to assert WorkflowItem ADDED precedes AssistantMessageItem ADDED. Change 1 — full-screen landing-page ChatLauncher modal. Layout-only CSS in ChatLauncher.tsx (no @openai/chatkit npm bump; CDN web component declares width/height 100% and fills parent).
- Skills: think-before-act, file-change-planner
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (2 new decision-log entries reversing M1b leniency + recording Change 1)
- Files: `apps/openclaw-bridge/app/openclaw_chatkit_server.py` (reorder respond() + except just-in-time assistant ADDED), `apps/openclaw-bridge/tests/test_progress_workflow.py` (T-3 strict ordering assertion), `apps/web/components/ChatLauncher.tsx` (outer `sm:p-4 md:p-6`, inner `w-full h-full`), `apps/web/scripts/launcher-fullscreen-check.mjs` (new — drawer-scoped iframe capture).
- Tests: 78/78 bridge tests passing incl. strengthened T-3. Curl smoke confirms `[03] workflow ADDED → [04] assistant_message ADDED`. Playwright at 375/768/1280 confirms launcher full-screen + workflow above prose.
- Status: done
- Outcome: Visible UX fixed. Both regressions resolved. Spec decision log updated to reverse the M1b "workflow can close in finally" leniency.
- Risks/TODOs: Found a 3rd-party env quirk while testing — landing page mounts TWO ChatKit instances (ChatDemo inline + launcher drawer); each gets its own iframe + chat session. Not a bug, but worth flagging if the demo + launcher ever need to share thread state.
- Rollback: `git restore` the 3 touched files; ACTIVITY_EVENTS=off remains the safety net for the bridge change.

[12:45] DEV TASK
- Source: Hub delegation (inter-session) — T-12 dev rollout + visual sign-off
- Type: feature (ACTIVITY_EVENTS=on flipped on dev; visual confirmation)
- Project: andy-ai-employee (`projects/andy-ai-employee/`)
- Brief: Added `ACTIVITY_EVENTS=on` to `apps/openclaw-bridge/.env`, triggered uvicorn reload, verified bridge emits `thread.item.added(workflow)` + `workflow.task.updated` in-place mutations via curl on /chatkit. Ran Playwright capture at 375/768/1280 against `/build/<uuid>` — workflow widget renders as collapsed dropdown with sparkle icon + "Working…" summary + chevron-right indicator (expanded=False confirmed visually). T-12 PASS.
- Skills: think-before-act, browsing-with-playwright (Playwright iframe drilling for CDN ChatKit)
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md`
- Files: `apps/openclaw-bridge/.env` (added ACTIVITY_EVENTS=on), `apps/web/scripts/t12-viewport-check.mjs` (new — Playwright smoke), `apps/web/scripts/probe-shadow.mjs` (new — debug), `specs/m1-t12-screenshots/t12-{375,768,1280}.png` (new — visual evidence).
- Tests: T-12 PASS at all 3 viewports. Curl smoke shows zero non-empty ProgressUpdateEvent text on flag-on path; workflow events flowing.
- Status: done (M1 fully shipped + live on dev)
- Outcome: Bridge live with workflow widget. No frontend code change required — existing ChatKitMount CDN web-component pattern just works. Observation: ChatLauncher tooltip overlaps composer at 375px (cosmetic, pre-existing, unrelated to workflow widget — separate ticket).
- Risks/TODOs: ChatKit CDN sandbox-iframe makes test automation awkward (had to drill `page.frame({name:"chatkit"})`); document for future tests. Multi-specialist relay timeline within one watch loop deferred to Phase 1.1.
- Rollback: `unset ACTIVITY_EVENTS` (or remove line from .env) → uvicorn reload → Phase 2 baseline restored.

[12:30] DEV TASK
- Source: Hub delegation (inter-session) — M1 close-out (M1b + M1c + M1d)
- Type: feature (M1 complete — realtime progress events shipped behind ACTIVITY_EVENTS flag)
- Project: andy-ai-employee (`projects/andy-ai-employee/apps/openclaw-bridge/`)
- Brief: Wire ProgressTracker into 6 emit sites of openclaw_chatkit_server.py. finally-block end_workflow. Watch loop accepts tracker. Tool-call complete detection on prose start. ACTIVITY_EVENTS=off baseline byte-identical to Phase 2 (with one latent-bug fix: icon="wrench" → "settings-slider" since wrench is no longer in ChatKit's icon allowlist). CDN-only frontend confirmed (zero @openai/chatkit* npm deps, CDN script in layout.tsx, web-component pattern in ChatKitMount.tsx).
- Skills: think-before-act, file-change-planner, security-auditor (zero new attack surface — documented ChatKit primitives only)
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (M1 complete, 3 new decision-log entries)
- Files: `app/openclaw_chatkit_server.py` (modified — 6 emit sites + tracker init + finally + tool-call complete detection + 1 pre-existing icon bug fix), `tests/test_progress_workflow.py` (extended — M1b integration tests + static guards)
- Tests: 78/78 bridge tests passing. Spec coverage: T-1..T-16 all green (T-12 manual viewport remains for visual confirmation post-flag-on flip). T-11 p99 = 14.4 µs.
- Status: done (M1 complete)
- Outcome: ACTIVITY_EVENTS=off default → byte-equivalent Phase 2. Flag-on → ChatKit-native WorkflowItem with in-place mutation, no duplication. Hard preserves (cancellation 1dde7f57, watch-loop a8f8205f) untouched and guarded by AST-level static tests.
- Risks/TODOs: T-12 manual viewport check pending (requires bridge + browser smoke once flag flipped on dev). Multi-specialist relay timeline within a single watch loop simplified to "one delegation task closes on first relay; subsequent relays unwrapped" — Phase 1.1 will refine.
- Rollback: set `ACTIVITY_EVENTS=off` (no redeploy). Code-level: `git restore` the 4 touched files.

[12:04] DEV TASK
- Source: Hub delegation (inter-session) — GREEN LIGHT for M1 build
- Type: feature (M1a — helper module + unit tests)
- Project: andy-ai-employee (`projects/andy-ai-employee/apps/openclaw-bridge/`)
- Brief: Implement ProgressTracker for ChatKit-native WorkflowItem emit. Lazy open, in-place mutation via WorkflowTaskUpdated, sub-1s coalesce, disabled-path no-op.
- Skills: think-before-act, file-change-planner, security-auditor (no new attack surface — documented ChatKit primitives only)
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (M1a)
- Files: `app/progress_workflow.py` (new), `tests/test_progress_workflow.py` (new)
- Tests: 10/10 passing — T-1, T-2, T-9, T-10, T-11, T-13, T-15 + 3 helper tests (disabled no-op, sub-1s coalesce, end_workflow finalize). T-11 p99 = 14.4 µs (target ≤ 500 µs).
- Status: done (M1a)
- Outcome: Helper module green. Next: M1b — wire 6 emit sites in openclaw_chatkit_server.py.
- Risks/TODOs: spec icons "wrench"/"users" not in ChatKit allowlist → swapped to "bolt"/"agent" (logged for M1b). Hard preserves (cancellation 1dde7f57, watch-loop a8f8205f) untouched.
- Rollback: `git rm app/progress_workflow.py tests/test_progress_workflow.py`

[14:00] DEV TASK
- Source: Hub delegation (inter-session via sessions_send from agent:main)
- Type: spec (write + backfill)
- Project: andy-ai-employee (`projects/andy-ai-employee/`)
- Brief: Spec realtime-progress-events (sentinel-tail in ProgressUpdateEvent, side-panel UI, Phase 1 only). Backfill ui-lovable-artifacts with Shipped Status section.
- Skills: think-before-act (implicit), spec-first discipline per AGENTS.md/SOUL.md
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (new) + `projects/andy-ai-employee/specs/ui-lovable-artifacts.md` (appended §23 Shipped Status)
- Files: 2 spec files; no code changes
- Tests: not run (spec-only phase; pre-implementation gate)
- Status: needs-approval (Hub confirm required before implementation)
- Outcome: Both specs delivered. realtime-progress-events covers all 15 SOUL.md sections with traceable AC↔test mapping. Backfill maps Phase 1 + Phase 2 spec items to shipped files + hotfix gates.
- Risks/TODOs: One blocking question to Hub — mobile bottom-sheet default behavior (collapsed-with-badge vs auto-open). Spec defaults to (b) collapsed-with-badge. Implementation gated on Hub confirm.
- Rollback: pure additive; spec files can be removed without code impact.

Key spec decisions locked in (from Hub):
- Transport: `::evt::<json>` sentinel inside `ProgressUpdateEvent.text` (not WidgetItem, not side-channel SSE). Strict client regex + JSON.parse + schema guard.
- Phase 2 deep subagent polling deferred (`ACTIVITY_DEEP_SUBAGENT=on` path not in this round).
- UI: dedicated side panel, responsive (≥1024 full / 768-1023 collapsible / <768 bottom sheet), existing theme tokens only.
- Hard preserves: cancellation fix (asyncio.wait pattern, session 1dde7f57) and watch-loop termination policy (session a8f8205f) — both have source-level guard tests (T-5, T-6).
- Debounce same-type pill events within 500ms; payload-bearing events (tool.end, subagent.relay) never debounced.
- Touch whitelist enforced: bridge respond + ChatKitMount + new ActivityFeed.tsx + parent page wire-up + 3 test files. Gateway/runtime/AGENTS/SOUL untouched.

[16:15] DEV TASK — M1 complete (realtime-progress-events bridge)
- Project: andy-ai-employee (apps/openclaw-bridge)
- Type: feature (Phase 1 M1 of 5)
- Brief: Bridge sentinel-tail emission for activity events. _progress helper + envelope schema + collision guard + 6 emit sites wired in respond() and _watch_for_handoff_relay.
- Files: apps/openclaw-bridge/app/openclaw_chatkit_server.py (modified), apps/openclaw-bridge/tests/test_progress_events.py (new). Spec §7/§11/§14 updated to lock option-b and default-off flags.
- Tests: 12/12 new pass; 72/72 total bridge tests pass.
- Live smoke: :8001 default-off shows zero sentinels (regression invariant ✓). Temp :8002 with ACTIVITY_EVENTS=on emitted 5 thinking envelopes across two requests, all parsed cleanly through client regex+schema, seq monotonic (1..7).
- Caveat: tool.start/tool.end + subagent.spawn/subagent.active/subagent.relay are wired and unit-tested via fake stream (T-15), but OpenClaw gateway translates Claude tool-use server-side and does NOT surface OpenAI-style tool_calls deltas, so those emit sites can't fire via live curl with this gateway shape. Code path is exercised in tests; live observation will come from handoff scenarios (specialist subagent dispatch) where the watch-loop path fires.
- Pre-existing icon name "wrench" was invalid per ChatKit Pydantic literal (would have crashed if ever hit in production). Replaced with "bolt" (tool.start) and "chevron-right" (subagent.relay) — both in allowed list.
- Status: needs-approval (M1 → push to Hub → await confirm or autonomous M2 per directive)
