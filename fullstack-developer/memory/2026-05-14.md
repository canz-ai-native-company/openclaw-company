# 2026-05-14 — Daily Log

[17:10] DEV TASK
- Source: Hub delegation (inter-session) — Production deploy of yesterday's frontend work
- Type: deploy (frontend only, Vercel prod)
- Project: andy-ai-employee (`projects/andy-ai-employee/apps/web/`)
- Brief: Ship the 2026-05-13 PM follow-up (dedicated `/chatkit` route + ChatLauncher rewrite + Nav/Hero `<Link href="/chatkit">` rewires + bridge timestamp fix). Backend (`apps/openclaw-bridge/`) untouched per scope — production already uses HF Space (`rrizwan98-openclaw-bridge.hf.space`).
- Skills: think-before-act, file-change-planner
- Spec: `projects/andy-ai-employee/specs/realtime-progress-events.md` (no edits — yesterday's §17 + decision-log entries cover the shipped work)
- Files: zero source files touched today; deploy-only. New script `apps/web/scripts/prod-smoke.mjs` for the post-deploy capture. New `specs/m1-t12-screenshots/prod-*.png` (6 screenshots).
- Tests: pre-flight `pnpm run typecheck` + `pnpm run lint` + `pnpm run build` (with `NEXT_PUBLIC_SITE_URL=https://andy.example.com` placeholder to bypass the `lib/seo.ts:4-13` localhost guard) all green. Curl prod home + `/chatkit` → 200 OK. Playwright 375/768/1280 home + `/chatkit` confirms `customElements.get("openai-chatkit")` truthy on both routes.
- Deploy: `dpl_5EADRg6v1pLvkq12FNgDeW4LAAbF`, alias `https://web-two-beryl-46.vercel.app`, 46s build duration.
- Status: done (live on prod)
- Outcome: All 4 milestones (pre-flight, env audit, deploy, smoke) clean. Vercel cloud build picked up production env (HF bridge URL + `domain_pk_...` ChatKit domain key + non-localhost SITE_URL). CSP correctly whitelists `*.hf.space` in `connect-src` and `cdn.platform.openai.com` in `script-src`/`frame-src`. CDN-only ChatKit constraint preserved (zero `@openai/chatkit*` npm deps in deployed bundle).
- Risks/TODOs: Per-deploy URL (`web-kt5e3e2cx-...vercel.app`) returns 401 due to Vercel team-SSO protection — only the alias (`web-two-beryl-46.vercel.app`) is public. Expected behavior for Vercel team plans. ChatLauncher hint popup overlap at 375 (pre-existing, noted earlier, separate ticket).
- Gotcha: `vercel env ls` / `vercel link` need cwd = `apps/web/`. Earlier ran from workspace root → CLI created a stray `.vercel/` at the root linked to the same project. Cleaned it up immediately; flagged the cwd discipline for future deploys.
- Rollback: Vercel dashboard → previous deployment → promote, OR `vercel rollback` CLI. No git churn (deploy was CLI-direct from filesystem).
