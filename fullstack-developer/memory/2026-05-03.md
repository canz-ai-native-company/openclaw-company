[21:48] DEV TASK
- Source: Hub delegation (subagent: agency-pilot-500-recurrence-round3)
- Task type: bugfix (infra/env) — round 3, eliminating the bug class after Round-2 patched the runtime
- Project: agency-pilot-canz (projects/agency-pilot-canz)
- Brief: Hub forwarded "still broken" report w/ logs at 12:39:28. Investigated and confirmed those logs are container-UTC = 17:39 PKT (pre-Round-2). Round-2 fix at 21:13 still held; bug was NOT actually re-occurring. Took the opportunity to apply the long-term fix Round-2 had only proposed.
- Skills used: think-before-act, file-change-planner, definition-of-done (real-endpoint verification, not /health alone)
- Spec path: projects/agency-pilot-canz/specs/fix-stale-container-database-url/spec.md (UPDATED — added "Round 3" section, ticked the long-term acceptance criterion, replaced the "Operational rule" follow-up with "RESOLVED in Round 3")
- Files changed:
  - projects/agency-pilot-canz/docker-compose.yml — dropped all `${VAR}` substitution lines from `environment:` block; only static `HOST=0.0.0.0`, `PORT=8000` remain; `env_file: .env` is now single source of truth. Added inline comment pointing future readers at the spec.
  - projects/agency-pilot-canz/specs/fix-stale-container-database-url/spec.md — Round-3 entry + criteria/follow-up updates
  - Backup: docker-compose.yml.bak.round3.20260503-2151
- Tests / verification (post-recreate, clean shell `env -i bash -c '...'`):
  - docker exec env | grep DATABASE_URL → ep-patient-meadow.../agency_pilot ✅
  - GET /health → connected:true ✅
  - POST /api/v1/agency-pilot/run with raza_agentic_ai_20260503_123655 → 400 "No competitors found" (DB-side gone; downstream OpenAI 429 + SerpAPI normalizer, both pre-documented) ✅
  - Container logs after request: ZERO `relation "super_*" does not exist` ✅
  - super_app/super_errors/super_lead all present in agency_pilot DB; client row intact ✅
- Status: done
- Outcome: Round-2 was correct end-state but only patched the runtime. Round-3 removes the `${VAR}` substitution that allowed shell shadow → `.env` is now single source of truth, and `docker compose up -d` is safe regardless of shell state. The "super_app does not exist" recurrence path is closed at the configuration level.
- Key finding: Pasted "recurrence" logs were stale UTC timestamps from before Round-2. Initially looked like a regression; was actually evidence Hub/user couldn't easily distinguish container-UTC from PKT. Verified by (a) container clock = UTC, (b) container uptime = 35 min (post-Round-2), (c) reproducing the endpoint = same Round-2 400 state.
- Side-effects (intentional cleanup):
  - SERPAPI_KEY now flows from .env → container directly (compose block had been forwarding misnamed `SERPAPI_API_KEY=${SERPAPI_API_KEY:-}` which was always empty). Code reading `SERPAPI_KEY` is unblocked.
  - S3_BUCKET_NAME no longer in container env (was never in .env; previously substituted to "" via shell). Add to .env if S3 upload is needed.
- Risks/TODOs:
  - SERPAPI_API_KEY vs SERPAPI_KEY naming: audit code for both, consolidate
  - S3_BUCKET_NAME: add to .env (and .env.example already lists it as optional)
  - Normalizer.py competitor list-vs-str validation bug (carried over from Round-2)
  - OpenAI quota — separate billing fix (carried over from Round-2)
  - First /health after long Neon idle returns "Connection timeout" (compute cold-start). Consider warming or treating one-shot transient as not-degraded.
- Backup/rollback: `cp docker-compose.yml.bak.round3.20260503-2151 docker-compose.yml && docker compose up -d --force-recreate radar-api`
- LESSON LEARNED (Round 3): Pasted error logs need timezone context. Container clock = UTC by default; PKT timestamps in user reports vs UTC timestamps in container logs differ by 5h. Always reconcile clock skew against `docker exec ... date` before treating a log as "live".



[21:13] DEV TASK
- Source: Hub delegation (subagent: agency-pilot-500-recurrence)
- Task type: bugfix (infra/env) — round 2, fixing my own false-fix from 17:44
- Project: agency-pilot-canz (projects/agency-pilot-canz)
- Brief: agency-pilot 500 ("super_app does not exist") came back 3 min after I claimed fixed at 17:44. Real fix this time.
- Skills used: think-before-act, file-change-planner, definition-of-done (verification discipline)
- Spec path: projects/agency-pilot-canz/specs/fix-stale-container-database-url/spec.md (UPDATED — added Round-1 post-mortem + corrected fix procedure)
- Files changed: 1 (spec update only — no code/compose changes yet; long-term fix proposed in Follow-ups)
- Tests:
  - docker exec env | grep DATABASE_URL → ep-patient-meadow.../agency_pilot ✅
  - GET /health → connected:true ✅
  - POST /api/v1/agency-pilot/run with raza_agentic_ai_20260503_123655 → 400 "No competitors found" (DB-side gone; downstream 400 is OpenAI 429 + normalizer schema bug, unrelated)
- Status: done (DB-side bug fully fixed and verified end-to-end this time)
- Outcome: Round-1 fix at 17:44 used `docker compose up -d --force-recreate radar-api` but invoking shell had stale `DATABASE_URL` exported. Compose `${VAR}` substitution let shell env shadow .env, so the recreated container baked in the OLD neondb URL. Round-2 fix: `unset DATABASE_URL` + `down` + `up`. Verified via direct endpoint call (NOT just /health — /health passed for round-1 too because old URL was reachable, just empty).
- Root cause: docker-compose.yml uses `environment: - DATABASE_URL=${DATABASE_URL}` with shell-env precedence over env_file. Shell had stale export.
- Risks/TODOs:
  - Long-term: drop `${VAR}` lines from compose env block; rely on `env_file: .env` only (single source of truth)
  - Add /health/db-schema endpoint that validates table list at startup
  - Fix normalizer.py: NormalizedCompetitor.business_type/business_description must accept str|list[str] (SerpAPI returns lists for some places, drops them all on the floor right now)
  - OpenAI key still over quota — separate billing fix
- Backup/rollback: no code/DDL changed; rollback = revert spec.md edit
- LESSON LEARNED: Never close a DB-error ticket on /health alone. /health confirms TCP/auth, not schema. Always run the actual failing endpoint with original payload before claiming "fixed".



# 2026-05-03

[17:37] DEV TASK
- Source: Hub delegation (subagent: insert-client-raza-agentic-ai)
- Task type: feature (one-off DB insert + helper script)
- Project: agency-pilot-canz (~/.openclaw/workspace/fullstack-developer/projects/agency-pilot-canz)
- Brief: Insert new client (Raza, Agentic AI niche, Austin/TX) into super_app and return real client_id for endpoint testing
- Skills used: think-before-act, file-change-planner (light)
- Spec path: none — pure insert task, no schema/contract change
- Files changed: 1 — scripts/insert_raza_client.py (new)
- Tests: n/a (one-off script ran successfully against Neon)
- Status: done
- Outcome: Inserted row, returned client_id `raza_agentic_ai_20260503_123655`. Set ni_chosen_niche_name and flag_deleted=FALSE so /api/v1/agency-pilot/run will accept it.
- Risks/TODOs:
  - DB schema drift: migration 001 declares super_app.id as UUID but live Neon DB has integer id. Worth investigating (separate task).
  - No "create client" REST endpoint exists; clients are inserted by internal scripts/services only. Future work could expose POST /api/v1/clients.
- Backup/rollback: To remove: `DELETE FROM public.super_app WHERE client_id = 'raza_agentic_ai_20260503_123655';`
