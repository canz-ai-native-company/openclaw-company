# 2026-05-04

[14:58] DEV TASK
- Source: Hub delegation (subagent: migrate-openai-key-env-to-db, requested by Raza)
- Type: feature (secrets migration — .env → DB primary, .env retained as fallback)
- Project: agency-pilot-canz (projects/agency-pilot-canz)
- Brief: OPENAI_API_KEY ko `.env` se Neon `agency_pilot.public.app_config` table mein migrate kiya (idempotent UPSERT), aur marker-swap test se prove kiya k `AppConfigRepository.get_openai_api_key()` ab DB se return kar raha hai. `.env` mein key untouched (fallback).
- Skills used: think-before-act, file-change-planner, security-auditor (mental pass), definition-of-done
- Spec: projects/agency-pilot-canz/specs/seed-openai-key-to-db/spec.md (NEW)
- Files changed (3 new, 0 modified):
  - projects/agency-pilot-canz/specs/seed-openai-key-to-db/spec.md
  - projects/agency-pilot-canz/scripts/seed_openai_key_to_db.py — idempotent UPSERT (asyncpg direct, mirrors run_migration.py pattern)
  - projects/agency-pilot-canz/scripts/verify_openai_key_from_db.py — marker-swap test using existing init_db / AppConfigRepository
- Verification (executed):
  - Seed run #1 → INSERTED, length=164, masked=sk-proj-M7...rxkA
  - Seed run #2 → UPDATED (idempotency proved)
  - Verify → marker `DBMARKER-b145d502...` injected → loaded value == marker (≠ .env) → DB-sourced ✅; original restored; final reload matches original. RESULT: PASS
- Status: done
- Outcome: DB is now primary source of OPENAI_API_KEY for agency-pilot-canz. `.env` retained as automatic fallback (via existing AppConfigRepository code path). No source code touched; only scripts + spec added.
- Hard constraints honoured:
  - ✅ Only `agency-pilot-canz` touched
  - ✅ Real key value never echoed/printed (only masked + synthetic marker)
  - ✅ `.env` not modified
  - ✅ Reused existing connection.py / AppConfigRepository (no new DB logic)
  - ✅ No git commit
- Risks/TODOs:
  - SERPAPI_KEY and FIRECRAWL_API_KEY still .env-only; same pattern can be applied later if Raza wants.
  - Marker-swap test temporarily writes a synthetic value to prod row — restored in finally; if process is killed mid-test, recovery branch prints masked original so operator can re-run seed.
- Rollback: `DELETE FROM public.app_config WHERE config_key = 'OPENAI_API_KEY';` → `.env` fallback resumes automatically (no code change needed).

---

[HH:MM] DEV TASK
- Source: Hub delegation (subagent: agency-pilot, fix approved by Raza after audit)
- Type: bugfix (config/env-key wiring)
- Project: agency-pilot-canz (projects/agency-pilot-canz)
- Brief: MarketFallbackAgent ko CROAuditAgent / DesignAuditAgent jaisa DB-key-first pattern pe lao. Pipeline `self.api_key` ab `enrich_with_fallback` se ho ke `MarketFallbackAgent(api_key=...)` tak forward hota hai. `.env` fallback sirf jab DB key None ho.
- Skills used: think-before-act, file-change-planner, definition-of-done
- Spec: projects/agency-pilot-canz/specs/market-fallback-db-key-pattern/spec.md (NEW)
- Files changed (2):
  - src/core/agency_pilot/market_fallback_agent.py
    - Constructor (~L144): aligned to reference pattern (`if api_key is None: env else passed`), docstring updated, ValueError preserved
    - `enrich_with_fallback` (~L435): added `api_key: str | None = None` kwarg + forward to `MarketFallbackAgent(api_key=api_key)` (~L468)
  - src/core/agency_pilot/pipeline.py
    - Call site (~L301): passes `api_key=self.api_key` to `enrich_with_fallback`
- Tests:
  - Searched tests/ for `MarketFallbackAgent` / `enrich_with_fallback` → no existing coverage (confirmed via grep on tests dir)
  - Per Raza: nayi tests NAHI likhi (instruction tha "agar nahi hain to mention karo")
  - Verification done: `python3 -m py_compile` on both files → PY_COMPILE_OK
- Status: done
- Outcome: Fallback agent ab pipeline ke baaki agents (CRO, Design, Research, Ranking, etc.) ke saath consistent. DB key chain unbroken; .env fallback intact; ValueError behaviour preserved.
- Hard constraints honoured:
  - ✅ Sirf 2 files touched (market_fallback_agent.py, pipeline.py + new spec doc)
  - ✅ `set_default_openai_key()` flow untouched
  - ✅ Other agents' key-loading patterns untouched
  - ✅ `app_config` repo / `get_openai_api_key()` untouched
- Notes:
  - Repo mid-rename: `src/core/radar/` git-tracked-as-deleted; `src/core/agency_pilot/` is untracked on disk. My edits land on the live `agency_pilot/` files. Eventual git-add of `agency_pilot/` will pick up these edits.
  - Behavioural change vs old `or` form: passing literal empty string `""` now raises ValueError instead of silently falling back to env. Reference pattern matches; no caller passes empty string.
- Risks/TODOs:
  - When repo finally `git add`s the rename, ensure these edits are part of the staged content (verify before commit)
  - Optional follow-up: add a unit test for the DB-key-first contract once Raza approves test additions
- Rollback:
  ```
  git checkout HEAD -- src/core/agency_pilot/market_fallback_agent.py src/core/agency_pilot/pipeline.py
  rm -rf specs/market-fallback-db-key-pattern/
  ```
  (Note: agency_pilot files currently untracked, so checkout would only undo if/after they're committed. Alternative rollback: revert the two edits manually.)
